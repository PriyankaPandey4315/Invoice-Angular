import { NgModule } from "@angular/core";
import { GraphsRoutingModule } from "./graphs.routing";
import { CommonModule } from "@angular/common";

@NgModule({
    imports:[
        GraphsRoutingModule,
        CommonModule
    ]
})
export class GraphsModule{}